import { RainbowColorDirective } from './rainbow-color.directive';

describe('RainbowColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowColorDirective();
    expect(directive).toBeTruthy();
  });
});
